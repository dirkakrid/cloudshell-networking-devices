language: python
python:
    - "2.7"

install:
  - pip install -r dev-requirements.txt
  - pip install -r test_requirements.txt

script:
    - python setup.py develop
    - nosetests --with-coverage --cover-package cloudshell/devices

after_success:
    - coveralls
